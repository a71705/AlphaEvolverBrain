# .dockerignore
# 此文件用于指定在执行 docker build 时应当忽略的文件和目录
# 这样可以减小构建上下文的大小，加快构建速度，并避免将不必要或敏感文件包含到镜像中

# Git 相关文件
.git
.gitignore

# Python 缓存文件
__pycache__/
*.pyc
*.pyo
*.pyd

# 虚拟环境目录 (常见的名称)
.venv/
venv/
env/
ENV/

# IDE 和编辑器配置文件
.vscode/
.idea/
*.swp
*.swo

# Docker 相关文件 (避免递归包含)
Dockerfile
docker-compose.yml
.dockerignore

# 操作系统生成的文件
.DS_Store
Thumbs.db

# 测试相关文件 (如果测试不需要在生产镜像中)
# tests/
# .pytest_cache/
# coverage.xml
# htmlcov/

# 本地开发配置文件 (例如包含敏感信息的 .env 文件)
# 注意: .env 文件通常用于本地开发，不应直接复制到生产镜像中。
# 环境变量应通过 Docker Compose 的 environment 或 secrets 机制注入。
.env

# 其他特定于项目的文件或目录
# 例如:
# data/ # 如果数据目录不应包含在镜像中
# logs/ # 日志文件通常由容器外部管理或通过卷挂载
